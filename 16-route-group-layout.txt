Route Group Layout

Route Group uses:

To organize your project in a manner that doesn't affect the URL.
To selectively apply a layout to certain segments while leaving others unchanged.
create a with-auth-layout folder inside auth folder and add login, register folder inside with-auth-layout folder.
and also create a layout.tsx file.